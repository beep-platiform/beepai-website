import { useEffect, useMemo, useState } from "react";
import {
  ArrowRight,
  BarChart3,
  Bot,
  ChevronRight,
  Clock3,
  Check,
  ChevronDown,
  CircleHelp,
  Cloud,
  Download,
  FileSpreadsheet,
  Globe2,
  Link2,
  LockKeyhole,
  Mail,
  Menu,
  MessageSquare,
  Play,
  ShieldCheck,
  Sparkles,
  Workflow,
  X,
  Zap,
} from "lucide-react";
import { fallbackPlans, loadPlans, loadPublicContent, type Plan, type SiteContent } from "./lib/supabase";
import { localeOptions, useI18n, type Locale } from "./lib/i18n";

const icons = { privacy: LockKeyhole, workflow: Workflow, devices: Cloud, support: CircleHelp };

function Logo({ compact = false }: { compact?: boolean }) {
  return <a className={`logo ${compact ? "logo-compact" : ""}`} href="/" aria-label="Beep home"><span className="logo-word">Beep</span><span className="logo-signal">◔</span></a>;
}

function Button({ children, href, variant = "primary", onClick }: { children: React.ReactNode; href?: string; variant?: "primary" | "ghost" | "light" | "aqua"; onClick?: () => void }) {
  const content = <>{children}<ArrowRight size={16} /></>;
  if (href) return <a className={`button button-${variant}`} href={href}>{content}</a>;
  return <button className={`button button-${variant}`} onClick={onClick}>{content}</button>;
}

const publicLinks = [["About", "/about"], ["Features", "/features"], ["How it works", "/how-it-works"], ["FAQ", "/faq"]];

function HeaderDropdown({ label, items }: { label: string; items: Array<{ label: string; href: string }> }) { const [open, setOpen] = useState(false); return <div className="nav-dropdown"><button className="nav-dropdown-trigger" onClick={() => setOpen(!open)} aria-expanded={open}>{label} <ChevronDown size={13} /></button>{open && <div className="nav-dropdown-menu">{items.map((item) => <a key={item.href} href={item.href} onClick={() => setOpen(false)}>{item.label}<ArrowRight size={13} /></a>)}</div>}</div>; }

function LanguageMenu() { const { locale, setLocale, t } = useI18n(); const [open, setOpen] = useState(false); return <div className="language-menu"><button onClick={() => setOpen(!open)} aria-expanded={open}>◉ &nbsp; {t("language")} <ChevronDown size={13} /></button>{open && <div className="language-options">{localeOptions.map((option) => <button key={option.id} className={locale === option.id ? "selected" : ""} onClick={() => { setLocale(option.id as Locale); setOpen(false); }}>{option.nativeLabel}<span>{option.id.toUpperCase()}</span></button>)}</div>}</div>; }

function PublicNav() {
  const [open, setOpen] = useState(false);
  const [contrast, setContrast] = useState(false);
  const { t } = useI18n();
  const toggleContrast = () => { setContrast(!contrast); document.body.classList.toggle("high-contrast", !contrast); };
  return <header className="site-header"><div className="utility-bar"><div className="utility-inner"><LanguageMenu /><button onClick={toggleContrast}><span className="contrast-switch"><span /></span> {t("highContrast")}</button><a href="/contact"><CircleHelp size={14} /> {t("contactSupport")}</a><div className="utility-spacer" /><a href="/admin/login"><span className="utility-person">◉</span> {t("login")}</a><a href="/contact">◉ &nbsp; {t("customerSupport")}</a><a href="/about">{t("about")} <ChevronDown size={13} /></a></div></div><div className="main-nav"><div className="nav-inner"><Logo /><nav className={open ? "nav-links nav-open" : "nav-links"}><HeaderDropdown label={t("product")} items={[{ label: t("about"), href: "/about" }, { label: t("features"), href: "/features" }, { label: t("howItWorks"), href: "/how-it-works" }, { label: t("getApp"), href: "/download" }]} /><a href="/how-it-works">{t("howItWorks")}</a><HeaderDropdown label={t("features")} items={[{ label: t("smartAutomation"), href: "/features" }, { label: t("platform"), href: "/features" }, { label: t("secureReliable"), href: "/privacy" }]} /><HeaderDropdown label={t("resources")} items={[{ label: "FAQ", href: "/faq" }, { label: t("contactSupport"), href: "/contact" }, { label: "Privacy", href: "/privacy" }]} /><a href="/#plans">{t("pricing")}</a></nav><div className="nav-actions"><Button href="/download" variant="aqua">{t("getApp")}</Button><a className="nav-soon" href="/download">{t("comingSoon")}</a></div><button className="menu-toggle" onClick={() => setOpen(!open)} aria-label={t("menu")}>{open ? <X /> : <Menu />}</button></div></div></header>;
}

function SectionKicker({ children }: { children: React.ReactNode }) { return <div className="section-kicker"><span className="kicker-line" />{children}</div>; }

function ProductPreview() {
  return <div className="hero-visual"><div className="visual-grid" /><div className="product-window"><div className="window-top"><span className="window-dots"><i /><i /><i /></span><span className="window-title">Beep / Overview</span><span className="window-live"><span />Live</span></div><div className="window-body"><aside><div className="mini-logo"><span className="logo-mark">B</span> Beep</div><div className="side-item active"><BarChart3 size={14} /> Overview</div><div className="side-item"><Workflow size={14} /> Automations</div><div className="side-item"><Zap size={14} /> Activity</div><div className="side-item"><ShieldCheck size={14} /> Privacy</div></aside><main className="window-main"><div className="window-greeting"><div><span className="muted-label">TUESDAY, 09:42</span><h3>Good morning, Jean.</h3></div><div className="avatar">J</div></div><div className="window-metrics"><div><strong>5</strong><span>Automations</span></div><div><strong>12</strong><span>Runs this week</span></div><div><strong className="mint">98%</strong><span>Success rate</span></div></div><div className="window-section-title"><span>Recent automations</span><span className="muted-label">VIEW ALL</span></div><div className="window-list"><div><span className="list-icon green"><FileSpreadsheet size={14} /></span><span><b>Monthly report generator</b><small>Last run · Today, 08:30</small></span><span className="status">Success</span></div><div><span className="list-icon blue"><MessageSquare size={14} /></span><span><b>WhatsApp daily reminder</b><small>Next run · Today, 18:00</small></span><span className="status">Scheduled</span></div><div><span className="list-icon violet"><Sparkles size={14} /></span><span><b>Invoice processor</b><small>Last run · Yesterday</small></span><span className="status">Success</span></div></div></main></div></div><div className="floating-chip chip-top"><ShieldCheck size={16} /><span><b>Private by default</b><small>Your data stays yours.</small></span></div><div className="floating-chip chip-bottom"><span className="pulse-dot" /><span><b>Working quietly</b><small>Runs when you need it.</small></span></div></div>;
}

function Hero({ content }: { content?: SiteContent }) {
  const { t } = useI18n();
  return <section className="hero"><div className="hero-glow" /><div className="container breadcrumb-bar"><a href="/">{t("home")}</a><ChevronRight size={15} /><a href="/features">{t("product")}</a><ChevronRight size={15} /><span>{t("automationApp")}</span></div><div className="container hero-grid"><ProductPreview /><div className="hero-copy"><SectionKicker>{t("automationForWork")}</SectionKicker><h1>{content?.title ?? t("heroTitle")}</h1><p className="hero-lede">{content?.body ?? t("heroBody")}</p><div className="hero-actions"><Button href="/download" variant="aqua">{t("getApp")}</Button><a className="text-link coming-link" href="/download">{t("comingSoon")}</a></div><ul className="hero-checks"><li><Check size={16} /> {t("connectServices")}</li><li><Check size={16} /> {t("automateTasks")}</li><li><Check size={16} /> {t("runTasks")}</li><li><Check size={16} /> {t("trackEverything")}</li><li><Check size={16} /> {t("stayNotified")}</li></ul></div></div></section>;
}

function Platform({ content }: { content?: SiteContent }) {
  const { t } = useI18n();
  const features = [{ icon: "privacy", title: "Private by design", text: "Your files and business data stay where they belong: with you. No permanent storage, no surprises." }, { icon: "workflow", title: "Made for real work", text: "Connect the tools you already use and turn a few small steps into one dependable flow." }, { icon: "devices", title: "Works on your terms", text: "Start on your computer, check in from your phone, and keep your workflow moving." }, { icon: "support", title: "Help when you need it", text: "Get clear guidance from a human team when you need to design something more bespoke." }];
  return <section className="section platform" id="platform"><div className="container"><SectionKicker>{t("platform")}</SectionKicker><div className="section-heading-row"><h2>{content?.title ?? t("platformTitle")}</h2><p>{content?.body ?? t("platformBody")}</p></div><div className="feature-grid">{features.map((feature) => { const Icon = icons[feature.icon as keyof typeof icons]; return <article className="feature-card" key={feature.title}><div className="feature-icon"><Icon size={20} /></div><h3>{feature.title}</h3><p>{feature.text}</p><a href="/features" className="card-arrow" aria-label={`Learn about ${feature.title}`}><ArrowRight size={16} /></a></article>; })}</div></div></section>; }

const workflowSteps = [{ number: "01", title: "Download Beep", text: "Get the Beep app on your phone or computer." }, { number: "02", title: "Connect your services", text: "Choose the services and tasks you want Beep to work with." }, { number: "03", title: "Describe your automation", text: "Tell Beep what should happen and when, in plain language." }, { number: "04", title: "Let Beep work", text: "Your automation runs while you focus on other things." }, { number: "05", title: "Track everything", text: "See activity, notifications, results, and errors from the app." }];

function WorkflowSection() { const { t } = useI18n(); return <section className="section workflow-section"><div className="container"><div className="workflow-layout"><div className="workflow-intro"><SectionKicker>{t("howItWorks")}</SectionKicker><h2>{t("automationForWork")}<br /><span>{t("trackEverything").split(" ").slice(-1)[0]}</span></h2><p>Automation should feel like a helpful teammate — clear, predictable, and quietly effective.</p><Button href="/how-it-works" variant="light">{t("seeHow")}</Button></div><div className="steps">{workflowSteps.slice(1, 4).map((step) => <div className="step" key={step.number}><span className="step-number">{step.number}</span><div><h3>{step.title}</h3><p>{step.text}</p></div><ArrowRight size={18} /></div>)}</div></div></div></section>; }

function Plans({ plans, live }: { plans: Plan[]; live: boolean }) {
  const [selected, setSelected] = useState("personal");
  const { t } = useI18n();
  return <section className="section plans-section" id="plans"><div className="container"><div className="plans-top"><div><SectionKicker>{t("pricing")}</SectionKicker><h2>{t("startSmall")}<br /><span>{t("goFurther")}</span></h2></div><div className="plans-note"><span className="live-badge"><span />{live ? t("livePricing") : t("previewCatalog")}</span><p>{t("planNote")}</p></div></div><div className="plans-grid">{plans.map((plan, index) => <article className={`plan-card ${selected === plan.id ? "plan-selected" : ""} ${index === 2 ? "plan-featured" : ""}`} key={plan.id} style={{ "--plan-accent": plan.accent } as React.CSSProperties}><div className="plan-card-top"><span className="plan-number">0{index + 1}</span><span className="plan-accent-dot" /></div><h3>{plan.name}</h3><p className="plan-summary">{plan.summary}</p><div className="plan-price"><strong>{plan.monthly_price_rwf === 0 ? "Free" : new Intl.NumberFormat("en-US").format(plan.monthly_price_rwf)}</strong>{plan.monthly_price_rwf > 0 && <span>RWF / month</span>}</div><div className="plan-divider" /><ul>{plan.features.map((feature) => <li key={feature}><Check size={15} />{feature}</li>)}</ul><button className={`plan-button ${selected === plan.id ? "selected" : ""}`} onClick={() => setSelected(plan.id)}>{selected === plan.id ? "Selected plan" : `Choose ${plan.name}`}<ArrowRight size={15} /></button></article>)}</div></div></section>;
}

function Help({ content }: { content?: SiteContent }) { const { t } = useI18n(); return <section className="section help-section" id="help"><div className="container"><div className="help-card"><div><SectionKicker>{t("help")}</SectionKicker><h2>{content?.title ?? t("helpTitle")}</h2><p>{content?.body ?? t("helpBody")}</p></div><div className="help-actions"><Button href="/contact" variant="light">{t("contactSupport")}</Button><a href="/faq">{t("faqLink")} <ArrowRight size={15} /></a></div></div></div></section>; }

function BenefitStrip() { const { t } = useI18n(); const benefits = [{ icon: Bot, title: t("smartAutomation"), text: t("smartAutomationText") }, { icon: Link2, title: t("connectAnything"), text: t("connectAnythingText") }, { icon: Clock3, title: t("saveTime"), text: t("saveTimeText") }, { icon: ShieldCheck, title: t("secureReliable"), text: t("secureReliableText") }]; return <section className="benefit-strip"><div className="container benefit-inner">{benefits.map(({ icon: Icon, title, text }, index) => <article className="benefit" key={title}><div className={`benefit-icon benefit-${index}`}><Icon size={24} /></div><div><h3>{title}</h3><p>{text}</p></div></article>)}</div></section>; }

function AppDownloadPanel() { const { t } = useI18n(); return <section className="app-download-panel"><div className="container app-download-inner"><div className="phone-art"><div className="phone-screen"><span className="logo-word">Beep</span><span className="logo-signal">◔</span><small>{t("yourWorkAutomated")}</small></div></div><div className="app-download-copy"><SectionKicker>{t("app")}</SectionKicker><h2>{t("pocketTitle")}<br /><span>{t("pocketTitleAccent")}</span></h2><p>{t("pocketBody")}</p></div><div className="store-cluster"><div><span>{t("availableAndroid")}</span><button className="store-button" disabled><span className="play-store-mark">▶</span><span><small>GET IT ON</small><b>Google Play</b></span></button></div><div><span>{t("availableIos")}</span><button className="store-button" disabled><span className="apple-mark">●</span><span><small>{t("comingSoon")}</small><b>App Store</b></span></button></div></div></div></section>; }

function Footer() { return <footer><div className="container footer-top"><Logo compact /><div className="footer-links"><a href="/about">About</a><a href="/features">Features</a><a href="/download">Download</a><a href="/privacy">Privacy</a><a href="/contact">Contact</a></div><span className="footer-tag">Your work. Automated. Privately.</span></div><div className="container footer-bottom"><span>© 2026 Beep</span><span>Built for people who make things happen.</span></div></footer>; }

function PageHero({ eyebrow, title, body }: { eyebrow: string; title: React.ReactNode; body: string }) { return <section className="page-hero"><div className="container narrow"><SectionKicker>{eyebrow}</SectionKicker><h1>{title}</h1><p>{body}</p></div></section>; }

function AboutPage() { return <><PageHero eyebrow="About Beep" title={<>Automation that feels<br /><em>human.</em></>} body="Beep exists to make everyday work lighter. We believe the best automation is clear enough to trust, useful enough to keep, and private enough to feel like yours." /><section className="section editorial-section"><div className="container editorial-grid"><div><SectionKicker>Why we built it</SectionKicker><h2>Less time<br /><span>managing work.</span></h2></div><div className="editorial-copy"><p>Small repetitive tasks add up. A report here, a reminder there, a file that needs moving before the day can finish. Beep helps you turn those moments into reliable routines.</p><p>The app is where automation happens. This website is here to explain it, help you get started, and give the Beep team a thoughtful place to support the product.</p><div className="quote">“The goal is not more software. It is more room for the work that matters.”</div></div></div></section></>; }

function FeaturesPage() { const items = [{ icon: Zap, title: "Task automation", text: "Describe the work you repeat and build a dependable routine around it." }, { icon: Workflow, title: "Workflow clarity", text: "Review what will happen before it runs, with each step visible and understandable." }, { icon: ShieldCheck, title: "Privacy first", text: "Beep is designed around local control, clear permissions, and minimal data retention." }, { icon: BarChart3, title: "Activity and results", text: "See what ran, what worked, and where a workflow needs your attention." }, { icon: Globe2, title: "Useful connections", text: "Bring the services you already use into one focused automation flow." }, { icon: CircleHelp, title: "Human support", text: "When a task is unique, our support team helps you find the right approach." }]; return <><PageHero eyebrow="What Beep can do" title={<>Make the work<br /><em>flow.</em></>} body="Beep handles the repeatable parts of your day so you can spend more attention on the parts that need you." /><section className="section feature-page"><div className="container"><div className="feature-list">{items.map(({ icon: Icon, title, text }, index) => <article key={title}><span className="feature-index">0{index + 1}</span><div className="feature-page-icon"><Icon size={23} /></div><div><h2>{title}</h2><p>{text}</p></div><ArrowRight size={20} /></article>)}</div></div></section></>; }

function HowItWorksPage() { return <><PageHero eyebrow="How to use Beep" title={<>Five steps to<br /><em>more breathing room.</em></>} body="Start with one repetitive task. Beep helps you connect it, describe it, run it, and understand what happened." /><section className="section how-page"><div className="container"><div className="long-steps">{workflowSteps.map((step) => <article key={step.number}><span className="step-number">{step.number}</span><div><h2>{step.title}</h2><p>{step.text}</p></div><span className="step-orb"><ArrowRight size={19} /></span></article>)}</div></div></section></>; }

function FaqPage() { const [open, setOpen] = useState(0); const faqs = [{ q: "What is Beep?", a: "Beep is an automation app for everyday work. It helps you turn repeatable tasks into workflows you can review, run, and track." }, { q: "What can Beep automate?", a: "Beep is designed for practical tasks such as spreadsheet calculations, reminders, reports, file organization, and connected service workflows." }, { q: "Is Beep free?", a: "Beep offers a free plan for simple tasks, with additional plans for people and teams that need more automations." }, { q: "Is Beep available on Android?", a: "The Android experience is coming soon. You can join the conversation through support to hear when the download becomes available." }, { q: "Does Beep work without internet?", a: "Some automations are designed for offline execution. The exact behavior depends on the services your workflow uses." }, { q: "Is my data safe?", a: "Beep is privacy-first by design. Permissions are explicit, execution is local where possible, and public content never asks visitors to create an account." }]; return <><PageHero eyebrow="Frequently asked questions" title={<>Answers, without<br /><em>the runaround.</em></>} body="A few useful answers before you download Beep. If you cannot find what you need, our support team is happy to help." /><section className="section faq-page"><div className="container faq-wrap">{faqs.map((faq, index) => <article className={open === index ? "faq-item faq-open" : "faq-item"} key={faq.q}><button onClick={() => setOpen(open === index ? -1 : index)}><span>{faq.q}</span><ChevronDown size={20} /></button>{open === index && <p>{faq.a}</p>}</article>)}</div></section></>; }

function DownloadPage() { return <><PageHero eyebrow="Get Beep" title={<>Your everyday tasks,<br /><em>automated.</em></>} body="The Beep app is where your automations live. Download links will activate as each platform becomes available." /><section className="section download-page"><div className="container download-grid"><article className="download-card download-android"><div className="download-icon"><Download size={25} /></div><span className="store-label">ANDROID</span><h2>Beep for Android</h2><p>Automate your daily tasks directly from your phone.</p><button className="store-button" disabled><span className="play-store-mark">▶</span><span><small>Available on</small><b>Google Play · Coming Soon</b></span></button></article><article className="download-card download-ios"><div className="download-icon"><Sparkles size={25} /></div><span className="store-label">IPHONE</span><h2>Beep for iOS</h2><p>Bring your automation workspace to iPhone when the release is ready.</p><button className="store-button" disabled><span className="apple-mark">●</span><span><small>Coming soon on</small><b>App Store</b></span></button></article></div></section></>; }

function ContactPage() { return <><PageHero eyebrow="Contact and support" title={<>Let’s make work<br /><em>lighter.</em></>} body="Questions about Beep, automation ideas, or a workflow that is not behaving as expected? We are here to help." /><section className="section contact-page"><div className="container contact-grid"><a className="contact-card" href="mailto:support@beepai.app"><Mail size={23} /><span>SUPPORT</span><h2>Get help with Beep</h2><p>support@beepai.app</p><ArrowRight size={18} /></a><a className="contact-card" href="mailto:hello@beepai.app"><MessageSquare size={23} /><span>GENERAL</span><h2>Talk about your workflow</h2><p>hello@beepai.app</p><ArrowRight size={18} /></a></div></section></>; }

function PrivacyPage() { return <><PageHero eyebrow="Privacy policy" title={<>Your data should<br /><em>stay yours.</em></>} body="Beep is built around a simple idea: automation should make work easier without making personal and business data harder to control." /><section className="section privacy-page"><div className="container privacy-grid"><div><SectionKicker>Our principles</SectionKicker><h2>Clear by<br /><span>default.</span></h2></div><div className="privacy-points">{["We explain what each permission is for.", "We prefer local and on-device execution where possible.", "We do not turn public website visits into user accounts.", "We keep the website focused on product information and support."].map((point) => <div key={point}><Check size={18} /><p>{point}</p></div>)}</div></div></section></>; }

function LoadingScreen() { const { t } = useI18n(); return <div className="loading-screen"><div className="loading-orbit"><span className="loading-dot" /><span className="loading-dot" /><span className="loading-dot" /></div><div className="loading-brand"><Logo /></div><p>{t("loading")}</p><small>{t("loadingDetail")}</small><div className="loading-bar"><span /></div></div>; }

function AdminLogin() { return <div className="admin-login"><div className="admin-login-card"><Logo /><div className="admin-lock"><LockKeyhole size={24} /></div><SectionKicker>Staff access</SectionKicker><h1>Welcome back.</h1><p>This area is for Beep administrators. Sign-in will be connected to Supabase Auth before sensitive actions are enabled.</p><a className="button button-aqua" href="/admin/dashboard">Enter control room <ArrowRight size={16} /></a><a className="back-public" href="/">← Back to public site</a></div></div>; }

function AdminPage({ route }: { route: string }) { const [status, setStatus] = useState("Ready"); const section = route.split("/")[2] || "dashboard"; const title = section === "dashboard" ? "Good morning, Jean." : section.charAt(0).toUpperCase() + section.slice(1); const summary = { notifications: "System updates and important signals across Beep.", requests: "Review automation and support requests from the Beep team.", messages: "Keep every support conversation in one place.", users: "Understand the people using Beep.", analytics: "A simple view of product activity and growth.", content: "Manage the public information visitors see.", settings: "Workspace settings and connected services." }[section as keyof Record<string, string>] ?? "Your Beep operations command center."; return <div className="admin-page"><aside className="admin-sidebar"><Logo /><div className="admin-nav"><span className="admin-nav-label">CONTROL ROOM</span>{[["dashboard", "Overview", BarChart3], ["notifications", "Notifications", Zap], ["requests", "Requests", Workflow], ["messages", "Messages", MessageSquare], ["users", "Users", Globe2], ["analytics", "Analytics", BarChart3], ["content", "Public content", Sparkles], ["settings", "Settings", ShieldCheck]].map(([key, label, Icon]) => <a key={key as string} className={section === key ? "admin-active" : ""} href={`/admin/${key}`}><Icon size={16} />{label as string}</a>)}</div><a className="back-public" href="/"><ArrowRight size={15} /> View public site</a></aside><main className="admin-main"><div className="admin-header"><div><SectionKicker>Private workspace</SectionKicker><h1>{title}</h1><p>{summary}</p></div><div className="admin-user"><span className="avatar">J</span><span><b>Jean Uwimana</b><small>Owner access</small></span><ChevronDown size={15} /></div></div><div className="admin-banner"><div className="banner-icon"><ShieldCheck size={21} /></div><div><b>Private route, public content stays separate</b><p>Google is blocked from this area. Connect Supabase Auth before enabling sensitive write actions.</p></div><span className="banner-status">{status}</span></div>{section === "dashboard" ? <><div className="admin-metrics"><div><span>Public plans</span><strong>4</strong><small className="positive">Live catalog</small></div><div><span>Active automations</span><strong>12</strong><small className="positive">+2 this month</small></div><div><span>Help requests</span><strong>7</strong><small>Needs review</small></div><div><span>Site status</span><strong className="status-live"><span />Live</strong><small>Supabase connected</small></div></div><section className="admin-grid"><div className="admin-panel"><div className="panel-heading"><div><span className="muted-label">CONTENT</span><h2>Plan catalog</h2></div><button onClick={() => setStatus("Saved")}>Save changes <ArrowRight size={14} /></button></div>{fallbackPlans.map((plan) => <div className="admin-row" key={plan.id}><span className="plan-accent-dot" style={{ background: plan.accent }} /><div><b>{plan.name}</b><small>{plan.monthly_price_rwf === 0 ? "Free" : `${plan.monthly_price_rwf.toLocaleString()} RWF / month`} · {plan.features.length} benefits</small></div><span className="row-live">Published</span><ArrowRight size={14} /></div>)}</div><div className="admin-panel"><div className="panel-heading"><div><span className="muted-label">INBOX</span><h2>Help requests</h2></div><a href="mailto:support@beepai.app">Open inbox <ArrowRight size={14} /></a></div>{["Excel to WhatsApp workflow", "Need help with schedules", "Business plan question"].map((item, index) => <div className="request" key={item}><span className={`request-avatar ${index === 1 ? "purple" : index === 2 ? "blue" : ""}`}>{["A", "M", "D"][index]}</span><div><b>{item}</b><small>{["Aline · 12 minutes ago", "Marcel · Yesterday", "David · Yesterday"][index]}</small></div><span className="request-tag">{index === 0 ? "New" : "Open"}</span></div>)}</div></section></> : <div className="admin-panel admin-empty"><div className="admin-empty-icon"><Sparkles size={24} /></div><h2>{title} is ready for your team.</h2><p>This section is connected to the Beep operations model and ready for Supabase-backed records.</p><button className="button button-aqua" onClick={() => setStatus("Saved")}>Mark as reviewed <ArrowRight size={16} /></button></div>}</main></div>; }

function App() {
  const [path, setPath] = useState(window.location.pathname.replace(/\/$/, "") || "/");
  const [plans, setPlans] = useState<Plan[]>(fallbackPlans);
  const [live, setLive] = useState(false);
  const [content, setContent] = useState<SiteContent[]>([]);
  const [loading, setLoading] = useState(true);
  useEffect(() => { const onPop = () => setPath(window.location.pathname.replace(/\/$/, "") || "/"); window.addEventListener("popstate", onPop); return () => window.removeEventListener("popstate", onPop); }, []);
  useEffect(() => { let active = true; const timeout = window.setTimeout(() => { if (active) setLoading(false); }, 1400); Promise.allSettled([loadPlans(), loadPublicContent()]).then(([plansResult, contentResult]) => { if (!active) return; if (plansResult.status === "fulfilled") { setPlans(plansResult.value.plans); setLive(plansResult.value.live); } if (contentResult.status === "fulfilled") setContent(contentResult.value.content); }).finally(() => { window.clearTimeout(timeout); if (active) setLoading(false); }); return () => { active = false; window.clearTimeout(timeout); }; }, []);
  useEffect(() => { const isAdmin = path.startsWith("/admin"); document.title = isAdmin ? "Control room · Beep" : "Beep — Automation for everyone"; let meta = document.querySelector('meta[name="robots"]'); if (!meta) { meta = document.createElement("meta"); meta.setAttribute("name", "robots"); document.head.appendChild(meta); } meta.setAttribute("content", isAdmin ? "noindex,nofollow,noarchive" : "index,follow"); }, [path]);
  if (loading) return <LoadingScreen />;
  if (path === "/admin/login") return <AdminLogin />;
  if (path.startsWith("/admin/")) return <AdminPage route={path} />;
  const bySlug = Object.fromEntries(content.map((item) => [item.slug, item]));
  const page = path === "/" ? <><PublicNav /><main><Hero content={bySlug.hero} /><BenefitStrip /><Platform content={bySlug.platform} /><WorkflowSection /><Plans plans={plans} live={live} /><AppDownloadPanel /><Help content={bySlug.help} /></main><Footer /></> : <><PublicNav /><main>{path === "/about" && <AboutPage />}{path === "/features" && <FeaturesPage />}{path === "/how-it-works" && <HowItWorksPage />}{path === "/faq" && <FaqPage />}{path === "/download" && <DownloadPage />}{path === "/contact" && <ContactPage />}{path === "/privacy" && <PrivacyPage />}{!["/about", "/features", "/how-it-works", "/faq", "/download", "/contact", "/privacy"].includes(path) && <AboutPage />}</main><Footer /></>;
  return page;
}

export default App;
